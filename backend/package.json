{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "NODE_ENV=test nodemon src/index.js",
    "startProd": "NODE_ENV=prod nodemon src/index.js",
    "test": "NODE_ENV=test node --experimental-vm-modules ./node_modules/.bin/jest",
    "prismaCreate": "dotenv -e .env.test -- npx prisma migrate dev --create-only",
    "prismaMigrate": "dotenv -e .env.test -- npx prisma migrate dev && npm run copyPrismaClient",
    "prismaMigrateProd": "dotenv -e .env.prod -- npx prisma migrate dev && npm run copyPrismaClient",
    "copyPrismaClient": "cp ./prisma/schema.prisma ../aws-cdk/lambda/schema.prisma && (cd ../aws-cdk/lambda && prisma generate)",
    "deploy": "npm run prismaMigrateProd && source .env.prod && docker build -t $IMAGE_NAME:latest . && docker tag $IMAGE_NAME:latest $AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$IMAGE_NAME:latest && aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com && docker push $AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$IMAGE_NAME:latest && docker images | grep $IMAGE_NAME | awk '{print $1\":\"$2}' | xargs docker rmi && aws ecs update-service --cluster $CLUSTER_NAME --service $SERVICE_NAME --force-new-deployment --task-definition $TASK_DEFINITION_ARN | cat",
    "prismaStudio": "dotenv -e .env.test -- npx prisma studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/server": "^4.4.0",
    "@aws-sdk/client-dynamodb": "^3.279.0",
    "@aws-sdk/client-s3": "^3.282.0",
    "@aws-sdk/cloudfront-signer": "^3.272.0",
    "@aws-sdk/lib-dynamodb": "^3.279.0",
    "@aws-sdk/s3-request-presigner": "^3.282.0",
    "@aws-sdk/util-dynamodb": "^3.281.0",
    "@prisma/client": "^4.11.0",
    "bcrypt": "^5.1.0",
    "dotenv": "^16.0.3",
    "jest": "^29.4.3",
    "joi": "^17.8.3",
    "jsonwebtoken": "^9.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "eslint": "^8.34.0",
    "eslint-config-standard": "^17.0.0",
    "eslint-plugin-import": "^2.27.5",
    "eslint-plugin-n": "^15.6.1",
    "eslint-plugin-promise": "^6.1.1",
    "prisma": "^4.11.0"
  },
  "type": "module"
}
