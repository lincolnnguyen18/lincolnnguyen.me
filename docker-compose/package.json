{
  "scripts": {
    "start": "docker-compose up -d",
    "stop": "docker-compose down",
    "restart": "npm run stop && npm run start",
    "ssh": "docker exec -it $ID sh",
    "rollingUpdate": "docker-compose up --force-recreate --no-deps -d api",
    "pullImages": "docker-compose pull",
    "fetchRepo": "git fetch --all && git reset --hard origin/main",
    "pruneImages": "docker image prune -a -f",
    "startAndPrune": "npm run start && npm run pruneImages",
    "pullAndUpdate": "npm run pullImages && npm run rollingUpdate"
  }
}